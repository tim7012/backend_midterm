<p>Title: <%= @post.title%></p>
<p><%= @post.content%></p>
<p>Category: <%= @post.category.try(:name)%></p>

<p><%= link_to "New Comment", new_post_comments_path(@post)%>

<br><br>

<table>
  <p>Comments:</p>
  <% @post.comments.each do |c|%>
    <tr>

      <td><%= c.content %></td>
      <td><%#= link_to "Delete", post_comments_path(@post), :method => :delete, :class => "btn btn-danger", :data => {:confirm => "Are you sure?"} %></td>

    </tr>
  <%end%>
</table>
<br><br>

<%= link_to "Back to Index", posts_path %>